# Κεφάλαιο 4
# Σχεδιασμός και υλοποίηση

## 4.1 Επισκόπηση συστήματος

Το σύστημα ETL που δημιουργήθηκε είναι μια πλατφόρμα επεξεργασίας δεδομένων σε πραγματικό χρόνο. Έχει χτιστεί πάνω σε Flink περιβάλλον και αλληλοεπιδρά με δύο ανεξάρτητες βοηθητικές υπηρεσίες που παράγουν τις εισερχόμενες ροές εισόδου. Το Flink Job που δημιουργήθηκε και εκτελείται σε Flink Cluster περιβάλλον είναι υπεύθυνο για τη συνένωση των δύο ροών και την επεξεργασία τους: την εφαρμογή δηλαδή των μετασχηματισμών που επιθυμεί να εφαρμόσει ο χρήστης και έχει εισάγει μέσω του αρχείου απαιτήσεων, και τέλος την αποθήκευση των τελικών αποτελεσμάτων σε μια μη σχεσιακή βάση δεδομένων MongoDB.

Το παρακάτω Διάγραμμα 1 παρουσιάζει την αρχιτεκτονική υψηλού επιπέδου. Τα Kafka topics `etl.config.v1` (configurations) και `etl.input.v1` (sensor events) τροφοδοτούν το Flink Job, το οποίο εκτελεί τους μετασχηματισμούς και αποθηκεύει τα αποτελέσματα στο topic `etl.output.v1`.

Διάγραμμα 1: Διάγραμμα υψηλού επιπέδου

> **[AD12]:** Γιατί οι task managers έχουν 4 slots each;

Στο Διάγραμμα 1 απεικονίζονται οι TaskManagers με 4 task slots έκαστος. Η επιλογή αυτή αντικατοπτρίζει το περιβάλλον ανάπτυξης (Docker Compose, κλάδος main), όπου ο βαθμός παραλληλισμού έχει οριστεί σταθερά στο 4 για σκοπούς τοπικής εκτέλεσης και ανάπτυξης. Στο πραγματικό κατανεμημένο περιβάλλον στο οποίο εκτελέστηκαν τα πειράματα της παρούσας εργασίας (SoftNet Cluster, Πολυτεχνείο Κρήτης), χρησιμοποιήθηκαν 11 μηχανήματα Dell PowerEdge R300 με 3 task slots το καθένα, δηλαδή συνολικά 33 διαθέσιμα task slots.

## 4.2 Flink Job

Το Flink Job είναι ο πυρήνας επεξεργασίας του συστήματος. Καταναλώνει τις δύο ροές δεδομένων από το Kafka: τα configurations του χρήστη από το topic `etl.config.v1` και τα sensor events από το topic `etl.input.v1`. Και οι δύο εισερχόμενες ροές χρησιμοποιούν το ίδιο κλειδί — τον τύπο του αισθητήρα — για να εκτελέσουν keyBy. Στη ροή των sensor events η εξαγωγή του κλειδιού ήταν απλή καθώς το πεδίο υπάρχει σε κάθε event. Η εξαγωγή του τύπου αισθητήρα από τη ροή των configurations ήταν πιο περίπλοκη, καθώς η παράμετρος `sensor`, αν και υποχρεωτική, δεν περιέχεται απαραίτητα σε κάθε μετασχηματισμό. Για τον λόγο αυτό δημιουργήθηκε ο `ConfigKeyExtractor`, που εξάγει τον τύπο αισθητήρα ως κλειδί από τον πρώτο μετασχηματισμό που περιέχει την παράμετρο `sensor`.

### 4.2.1 Flink Operators που χρησιμοποιήθηκαν

> **[AD13]:** Γενικώς, τους μετασχηματισμούς του Flink δεν τους έχει περιγράψει πουθενά.

Η υλοποίηση του Flink Job αξιοποιεί μια σειρά από ενσωματωμένους τελεστές (operators) του Apache Flink, οι οποίοι συνδέονται αλυσιδωτά σχηματίζοντας τον κατευθυνόμενο άκυκλο γράφο (DAG) επεξεργασίας. Στη συνέχεια περιγράφεται ο ρόλος κάθε τελεστή στο pipeline.

**addSource**: Αποτελεί το σημείο εισόδου κάθε ροής δεδομένων στο Flink Job. Χρησιμοποιείται δύο φορές: μία για τη ροή configurations (`FlinkKafkaConsumer` από το topic `etl.config.v1` με offset `earliest`, ώστε να αναγνωσθούν όλα τα configurations από την αρχή του topic κατά την εκκίνηση) και μία για τη ροή sensor events (`FlinkKafkaConsumer` από το topic `etl.input.v1` με offset `latest`, ώστε να επεξεργάζονται μόνο τα νέα events).

**map**: Χρησιμοποιείται για την αποσειριοποίηση (deserialization) των μηνυμάτων Kafka από raw JSON bytes σε τυποποιημένα αντικείμενα Java (`EtlConfig` για τα configurations, `SensorEvent` για τα sensor events). Η αποσειριοποίηση υλοποιείται μέσω custom `EventDeserializer` που χρησιμοποιεί manual JSON streaming parser (`JsonFactory`/`JsonParser`) για λόγους απόδοσης.

**keyBy**: Εφαρμόζεται και στις δύο ροές με κλειδί τον τύπο αισθητήρα (sensor type). Στη ροή sensor events το κλειδί εξάγεται άμεσα από το πεδίο `sensor` κάθε event. Στη ροή configurations το κλειδί εξάγεται από τον `ConfigKeyExtractor`. Η κοινή χρήση του ίδιου κλειδιού εξασφαλίζει ότι configurations και events του ίδιου τύπου αισθητήρα δρομολογούνται πάντα στο ίδιο parallel subtask.

**connect**: Συνδέει τις δύο keyed ροές (configurations και sensor events) σε μία ενιαία `ConnectedStreams`, επιτρέποντας την κοινή επεξεργασία τους από τον επόμενο τελεστή ενώ διατηρούνται διακριτές (κάθε ροή έχει τη δική της μέθοδο επεξεργασίας).

**flatMap (CoFlatMap)**: Αποτελεί τον κεντρικό τελεστή επεξεργασίας. Υλοποιείται μέσω `RichCoFlatMapFunction` και περιγράφεται αναλυτικά στην επόμενη ενότητα (§4.2.2).

**window (TumblingProcessingTimeWindows)**: Εφαρμόζεται στη ροή των αποτελεσμάτων που απαιτούν aggregation. Δημιουργεί tumbling windows σταθερής διάρκειας (3 δευτερολέπτων), οριζόμενης κατά την εκκίνηση του Flink Job. Κάθε event ανήκει σε ακριβώς ένα παράθυρο.

**aggregate**: Εκτελεί τον υπολογισμό συνάθροισης (sum, max, min, avg) εντός κάθε παραθύρου, ομαδοποιώντας τα αποτελέσματα βάσει σύνθετου κλειδιού (`groupingKey|jobId|aggregationType`) μέσω του `WindowAccumulator`.

**union**: Συνενώνει τη ροή των direct results (element transformations) με τη ροή των windowed results (aggregation transformations) σε μία ενιαία ροή εξόδου `EtlResult`.

**addSink**: Αποτελεί το σημείο εξόδου του Job. Χρησιμοποιεί `FlinkKafkaProducer` για δημοσίευση των αποτελεσμάτων στο topic `etl.output.v1`, από όπου ένα downstream service τα αποθηκεύει στη MongoDB. Κατά τα πειράματα (Κεφάλαιο 5), το sink τροποποιήθηκε σε Kafka sink απευθείας για μέτρηση απόδοσης.

### 4.2.2 Δυναμική Σύνδεση Ροών και Διαχείριση Κατάστασης Configurations

> **[AD14]:** Μετονομασία από "CoFlatMap Operator" σε "Δυναμική Σύνδεση Ροών και Διαχείριση Κατάστασης Configurations".

> **[AD15]:** Ξαναγράψε την πρώτη παράγραφο ώστε να εστιάζει στη λειτουργία που υλοποιεί, όχι στον τεχνικό τελεστή.

Ένα από τα βασικά ζητούμενα του συστήματος είναι η δυνατότητα δυναμικής εφαρμογής configurations χωρίς επανεκκίνηση της εφαρμογής. Κάθε νέο configuration που υποβάλλει ο χρήστης ενσωματώνεται άμεσα στη ροή επεξεργασίας, ενημερώνοντας τη διανεμημένη κατάσταση (distributed state) του συστήματος. Τα επόμενα sensor events αντιμετωπίζονται αυτόματα σύμφωνα με τους κανόνες του ενημερωμένου configuration, χωρίς να διακοπεί ή να επανεκκινηθεί η επεξεργασία. Αυτή η συμπεριφορά επιτυγχάνεται μέσω του μηχανισμού συνένωσης ροών (connected streams) που παρέχει το Flink, ο οποίος επιτρέπει δύο ροές να μοιράζονται κοινή κατάσταση (shared state) ενώ επεξεργάζονται ανεξάρτητα.

Η `CoFlatMapFunction` αποτελεί βασικό μηχανισμό του Flink για τη συνένωση δύο ροών δεδομένων (connected streams), όπου κάθε ροή επεξεργάζεται από ξεχωριστή μέθοδο αλλά μοιράζονται κοινή κατάσταση [7]. Ο `CoFlatMapProcessor` που υλοποιήθηκε χρησιμοποιεί την `RichCoFlatMapFunction`, η οποία είναι η εμπλουτισμένη έκδοση που παρέχει πρόσβαση σε managed state και lifecycle methods μέσω του `RuntimeContext` [8].

Στην περίπτωσή μας η χρήση της Rich έκδοσης παρέχει:

- **Managed State:** `MapState<String, EtlConfig>` για αποθήκευση configurations με κλειδί το `jobId`
- **Lifecycle method `open()`:** Αρχικοποίηση του state πριν την έναρξη επεξεργασίας δεδομένων

Η χρήση keyed state εξασφαλίζει ότι configurations και events με το ίδιο key επεξεργάζονται από τον ίδιο parallel task. Ένα configuration που αφορά π.χ. θερμοκρασία θα βρίσκεται πάντα στο ίδιο parallel subtask με τα events θερμοκρασίας, επιτρέποντας την άμεση πρόσβαση στο state χωρίς επικοινωνία μεταξύ tasks.

Η διαχείριση των δύο ροών, όπως απεικονίζεται στο Διάγραμμα 2, γίνεται από τις παρακάτω μεθόδους:

- **flatMap1 (EtlConfig):**
  - Το αρχείο απαιτήσεων αποθηκεύεται στο `MapState<String, EtlConfig>` με κλειδί το `jobId`.
  - Δημιουργούνται οι transformation `MapFunction` και αποθηκεύονται με caching με κλειδί το `jobId`.
  - Εκπέμπεται ένα `EtlResult` ως config status confirmation.

- **flatMap2 (SensorEvent):**
  - Για κάθε αρχείο απαιτήσεων στο state διαχωρίζονται τα transformations σε element και aggregation.
  - Εφαρμόζονται τα cached element transformation functions σειριακά. Αν κάποιο filter επιστρέψει null, το event κόβεται και προχωράει στο επόμενο config.
  - Αν το event περάσει τα φίλτρα και υπάρχουν aggregations, εκπέμπεται `EtlResult` για κάθε matching aggregation (windowed path).
  - Αν το event περάσει τα φίλτρα και δεν υπάρχουν aggregations, εκπέμπεται απευθείας ως `EtlResult` (direct path).

Διάγραμμα 2: CoFlatMap Operator

### 4.2.3 Εφαρμογή μετασχηματισμών

Σε αυτή την ενότητα περιγράφεται ο τρόπος με τον οποίο έχουν υλοποιηθεί οι μετασχηματισμοί. Ο χρήστης μπορεί να υποβάλει ένα ή περισσότερα αρχεία μετασχηματισμών. Κάθε αρχείο μπορεί να περιέχει έναν ή περισσότερους μετασχηματισμούς.

Το σύστημα υποστηρίζει δύο κατηγορίες μετασχηματισμών:

- **Element Transformations:** Τα element transformations εφαρμόζονται σειριακά ως αλυσίδα πάνω σε κάθε event. Η έξοδος κάθε transformation γίνεται η είσοδος του επόμενου. Οι transformation functions δημιουργούνται μία φορά (στο `flatMap1`) και αποθηκεύονται στο cache. Η σειρά εφαρμογής καθορίζεται από τη σειρά εμφάνισης στο configuration. Αν ένα φίλτρο απορρίψει την εγγραφή, η επεξεργασία σταματά για αυτό το configuration.

- **Aggregation Transformations:** Εφαρμόζονται σε ομάδες εγγραφών εντός του χρονικού παραθύρου που ορίζεται κατά την εκτέλεση της Flink διαδικασίας. Τα aggregation transformations (sum, max, min, avg) εφαρμόζονται μόνο σε events που πέρασαν επιτυχώς τα element transformations. Αν ένα event κοπεί από filter, δεν φτάνει ποτέ στα aggregations.

Η εφαρμογή μετασχηματισμών ακολουθεί διακριτά στάδια επεξεργασίας, όπως απεικονίζεται στο Διάγραμμα 3. Όταν ένα sensor event εισέρχεται στον CoFlatMap processor, πρώτα εφαρμόζονται τα element transformations που υπάρχουν αποθηκευμένα για κάθε αρχείο απαιτήσεων με τη σειρά που υπάρχουν στο αρχείο. Κάθε transformation λαμβάνει ως είσοδο το αποτέλεσμα του προηγούμενου, δημιουργώντας μια αλυσίδα επεξεργασίας.

Τα element transformations χωρίζονται σε τρεις κατηγορίες: τα **filters** (filter_greater, filter_less) αποφασίζουν αν η εγγραφή θα προχωρήσει στο επόμενο στάδιο ή θα απορριφθεί, το **normalize** μετασχηματίζει αριθμητικές τιμές σε κανονικοποιημένη κλίμακα [0,1], και τα **text operations** (to_lowercase, to_uppercase, trim_whitespace) επεξεργάζονται πεδία κειμένου. Αν ένα filter απορρίψει την εγγραφή, η επεξεργασία για τη συγκεκριμένη εγγραφή εισόδου σταματά.

Μετά την ολοκλήρωση των element transformations, οι εγγραφές που προορίζονται για aggregation εισέρχονται στο στάδιο windowing που περιγράφεται εκτενώς στην επόμενη ενότητα. Χρησιμοποιήθηκαν tumbling windows σταθερής διάρκειας, όπου κάθε εγγραφή ανήκει σε ακριβώς ένα παράθυρο. Εντός κάθε παραθύρου, οι εγγραφές ομαδοποιούνται με βάση το πεδίο `keyBy` από το αρχείο των απαιτήσεων — αν δεν έχει συμπληρωθεί από τον χρήστη, χρησιμοποιείται ως default ο τύπος αισθητήρα — και υπολογίζεται η ζητούμενη συνάθροιση. Το αποτέλεσμα εκπέμπεται όταν κλείσει το παράθυρο.

Τα element transformations εφαρμόζονται εντός του CoFlatMap. Τα aggregation transformations (sum, max, min, avg) απαιτούν windowing και εκτελούνται σε επόμενο στάδιο του pipeline. Μετά τον CoFlatMap, η ροή διαχωρίζεται με βάση το πεδίο `aggregationType` του `EtlResult`:

- **Results χωρίς aggregation:** Προωθούνται απευθείας στην έξοδο, μαζί με το μήνυμα κατάστασης του config *"Configuration Submit Successfully"*.
- **Results με aggregation:** Περνούν από `keyBy` → `Window` → `Aggregate` πριν γραφτούν στην έξοδο.

Τέλος, οι δύο ροές συνενώνονται μέσω union και γράφονται στην έξοδο, όπως φαίνεται και στο Διάγραμμα 3.

Διάγραμμα 3: Flink Processing Pipeline

### 4.2.4 Παράθυρα

Όπως αναφέρθηκε παραπάνω, χρησιμοποιήθηκαν tumbling windows σταθερού μεγέθους, όπως απεικονίζεται στο Διάγραμμα 4. Το μέγεθος του παραθύρου ορίζεται κατά την αρχικοποίηση του Flink Job. Τα aggregation results ομαδοποιούνται με ένα σύνθετο κλειδί (`groupingKey|jobId|aggregationType`) και εισέρχονται στο παράθυρο. Το grouping key ορίζεται ως παράμετρος από τον χρήστη στον aggregate μετασχηματισμό. Αν δεν υπάρχει, θεωρείται ως default η παράμετρος του αισθητήρα.

Με τη χρήση του παραπάνω σύνθετου κλειδιού εξασφαλίζεται ότι κάθε μοναδικός συνδυασμός ομαδοποίησης, εργασίας και τύπου συνάθροισης διατηρεί ανεξάρτητη κατάσταση (state) εντός του παραθύρου. Αυτό είναι απαραίτητο διότι ο `WindowAccumulator`, ο οποίος συσσωρεύει τις τιμές κατά τη διάρκεια του παραθύρου, προϋποθέτει ότι όλα τα στοιχεία που εισέρχονται σε αυτόν ανήκουν στην ίδια ομάδα και υπολογίζονται με τον ίδιο τύπο συνάθροισης. Χωρίς το σύνθετο κλειδί, δεδομένα από διαφορετικούς αισθητήρες ή διαφορετικές εργασίες θα αναμειγνύονταν στον ίδιο accumulator, οδηγώντας σε εσφαλμένα αποτελέσματα. Τέλος, ο `WindowAggregator` παράγει το τελικό αποτέλεσμα στο κλείσιμο κάθε παραθύρου.

Διάγραμμα 4: Μονοπάτι συναθροίσεων

### 4.2.5 Αποθήκευση αποτελεσμάτων

Τα τελικά αποτελέσματα που παράγονται για κάθε αρχείο απαιτήσεων αποθηκεύονται σε μια βάση δεδομένων MongoDB. Κάθε αρχείο / JobId αποτελεί ένα ξεχωριστό Collection στη βάση. Στην Εικόνα 1 παρατίθεται ένα δείγμα εξόδου, το οποίο αντιστοιχεί σε configuration που φιλτράρει θερμοκρασίες άνω των 25°C, μετατρέπει το πεδίο τοποθεσίας σε κεφαλαία και υπολογίζει τον μέσο όρο ανά τοποθεσία.

Εικόνα 1: Παράδειγμα εξόδου

Το αποτέλεσμα της Εικόνας 1 αποδίδει την τοποθεσία σε κεφαλαία (KITCHEN), καθώς και τον μέσο όρο θερμοκρασίας στη συγκεκριμένη τοποθεσία για θερμοκρασίες που ξεπερνούν τους 25°C, κατά το συγκεκριμένο παράθυρο χρόνου (windowStart – windowEnd).

## 4.3 Ροή Δεδομένων End-to-End

Η πλήρης ροή δεδομένων από την υποβολή configuration έως την αποθήκευση αποτελεσμάτων παρουσιάζεται στο Διάγραμμα 5 και περιλαμβάνει τα εξής βήματα:

1. **Config Submission:** Ο χρήστης στέλνει POST στο `/config` με JSON configuration. Το ETL API επικυρώνει και δημοσιεύει στο config topic.
2. **Data Generation:** Ο Producer δημιουργεί sensor events και τα δημοσιεύει στο input topic με configurable rate.
3. **Stream Processing:** Το Flink Job καταναλώνει και τα δύο streams. Ο CoFlatMap αποθηκεύει τα configs στο state και επεξεργάζεται τα events. Τα aggregations περνούν από windowing.
4. **Result Storage:** Τα αποτελέσματα αποθηκεύονται στη βάση δεδομένων MongoDB.

Διάγραμμα 5: End-to-end ροή δεδομένων

> **[AD17]:** Γενικώς, δεν αναφέρεσαι συχνά στο κείμενο στις εικόνες, στα διαγράμματα κτλ. → Διορθώθηκε: κάθε διάγραμμα/εικόνα αναφέρεται ρητά στο κείμενο πριν εμφανιστεί.

## 4.4 Βοηθητικές υπηρεσίες - Microservices

Ολόκληρη η εφαρμογή έχει στηθεί στη λογική των microservices. Εκτός από το Flink Job που αναλύθηκε παραπάνω, αναπτύχθηκαν ακόμα δύο βοηθητικές υπηρεσίες για την εξυπηρέτηση του σκοπού της εργασίας: η υπηρεσία File Producer που δημιουργεί τα δεδομένα των αισθητήρων, και η υπηρεσία ETL API που επιτρέπει στον χρήστη να υποβάλλει τα ερωτήματά του προς τη ροή των δεδομένων. Κάθε microservice εκτελείται σε ξεχωριστό Docker container και επικοινωνεί με τα υπόλοιπα μέσω Apache Kafka, όπως φαίνεται στο Διάγραμμα 6.

Διάγραμμα 6: Αρχιτεκτονική microservices

### 4.4.1 ETL API Service

Το ETL API service είναι ένα REST API υλοποιημένο με το framework Spring Boot. Αποτελεί το σημείο εισόδου του συστήματος για τους χρήστες. Δέχεται configurations σε JSON μορφή μέσω HTTP POST στο endpoint `/config`. Κάθε configuration περιέχει ένα μοναδικό jobId και μια λίστα μετασχηματισμών προς εφαρμογή.

Η υπηρεσία εκτελεί επικύρωση του configuration: ελέγχει ότι υπάρχει το jobId, ότι οι τύποι μετασχηματισμών είναι έγκυροι και ότι οι απαραίτητες παράμετροι υπάρχουν για κάθε τύπο. Μετά την επικύρωση, το configuration δημοσιεύεται στο Kafka στο config topic. Τα διαθέσιμα REST endpoints παρουσιάζονται στον Πίνακα 5.

| Endpoint | Method | Περιγραφή |
|----------|--------|-----------|
| /config | POST | Υποβολή configuration — επικύρωση και δημοσίευση στο Kafka |
| /health | GET | Health check για container orchestration |

*Πίνακας 5: REST endpoints του ETL API*

Στην Εικόνα 2 παρουσιάζεται ένα παράδειγμα πλήρους configuration file. Το συγκεκριμένο configuration φιλτράρει τις θερμοκρασίες μεγαλύτερες των 25°C, υπολογίζει τον μέσο όρο θερμοκρασίας ανά τοποθεσία και μετατρέπει το πεδίο της τοποθεσίας σε κεφαλαία γράμματα.

Εικόνα 2: Παράδειγμα πλήρους Configuration

### 4.4.2 File Producer Service

Η υπηρεσία File Producer παράγει δεδομένα αισθητήρων και τα δημοσιεύει στο input topic του Kafka. Πρόκειται ουσιαστικά για τη ροή εισόδου του συστήματος. Στόχος της είναι να προσομοιώσει ένα ρεαλιστικό δίκτυο αισθητήρων με 6 τύπους μετρήσεων (θερμοκρασία, πίεση, υγρασία, ποιότητα αέρα, φωτισμός και θόρυβος) σε 8 διαφορετικές τοποθεσίες.

Σε κάθε κύκλο παραγωγής, δημιουργούνται μετρήσεις για όλους τους συνδυασμούς αισθητήρων και τοποθεσιών, δηλαδή 48 μηνύματα (8 τοποθεσίες × 6 αισθητήρες). Οι τιμές των μετρήσεων ακολουθούν κανονική κατανομή (Gaussian) με ρεαλιστικά όρια για κάθε τύπο αισθητήρα. Κάθε μήνυμα περιλαμβάνει τον τύπο αισθητήρα, την τιμή μέτρησης, τη μονάδα μέτρησης, την τοποθεσία, το timestamp (αποθηκευμένο ως String ISO-8601) και έναν δείκτη ποιότητας δεδομένων που υπολογίζεται με βάση την απόκλιση από τις αναμενόμενες τιμές. Η μορφή ενός τυπικού μηνύματος εισόδου φαίνεται στην Εικόνα 3. Ο ρυθμός παραγωγής είναι παραμετροποιήσιμος μέσω environment variable (π.χ. `RATE_PER_SEC=10` για 10 κύκλους ανά δευτερόλεπτο, δηλαδή 480 μηνύματα/sec).

Εικόνα 3: Δεδομένα εισόδου

## 4.5 Docker Containerization

Η υποδομή βασίζεται σε Docker containers. Το Docker Compose ορίζει τις υπηρεσίες και τις εξαρτήσεις τους, όπως απεικονίζεται στο Διάγραμμα 7. Η στοίβα οργανώνεται σε δύο επίπεδα:

- **Infrastructure Layer:** Zookeeper, Kafka, MongoDB — οι υπηρεσίες υποστήριξης
- **Application Layer:** ETL API, File Producer, Flink — τα microservices της εφαρμογής

Διάγραμμα 7: Docker Compose stack

Η σειρά εκκίνησης καθορίζεται από τις εξαρτήσεις που απεικονίζονται στο Διάγραμμα 8: ο Zookeeper εκκινεί πρώτος, στη συνέχεια το Kafka, ακολουθεί το kafka-init (δημιουργία topics) και τέλος εκκινούν παράλληλα οι υπηρεσίες εφαρμογής (ETL API, Producer, Flink Job → MongoDB).

Διάγραμμα 8: Εξαρτήσεις υπηρεσιών

## 4.6 Τεχνολογικό Stack

> **[AD18]:** Υπερβολικά σύντομος — ανάπτυξη με αιτιολόγηση επιλογής κάθε τεχνολογίας.

Οι τεχνολογίες που χρησιμοποιήθηκαν στην υλοποίηση του συστήματος παρουσιάζονται συνοπτικά στον Πίνακα 6. Στη συνέχεια αιτιολογείται αναλυτικά η επιλογή κάθε τεχνολογίας.

| Επίπεδο | Τεχνολογία | Ρόλος |
|---------|-----------|-------|
| Runtime | Java | Γλώσσα προγραμματισμού |
| Framework | Spring Boot | REST API και Producer services |
| Stream Processing | Apache Flink | Stateful stream processing |
| Message Broker | Apache Kafka | Event streaming μεταξύ services |
| Coordination | Apache Zookeeper | Kafka cluster coordination |
| Database | MongoDB | Αποθήκευση αποτελεσμάτων |
| Containerization | Docker | Container runtime |
| Orchestration | Docker Compose | Multi-container deployment |

*Πίνακας 6: Τεχνολογικό stack*

**Apache Flink**

Το Apache Flink επιλέχθηκε ως η μηχανή επεξεργασίας ροών λόγω της native υποστήριξης stateful stream processing και του μηχανισμού checkpointing. Σε αντίθεση με batch systems που επεξεργάζονται δεδομένα σε ομάδες ανά τακτά χρονικά διαστήματα, το Flink επεξεργάζεται κάθε event μόλις αυτό φτάσει, χωρίς buffering, επιτυγχάνοντας εγγυημένα χαμηλή καθυστέρηση. Ο μηχανισμός keyed state επιτρέπει την αποθήκευση configurations ανά τύπο αισθητήρα σε κατανεμημένη κατάσταση, η οποία διατηρείται και ενημερώνεται δυναμικά κατά την εκτέλεση. Τα tumbling windows εξασφαλίζουν τον ορθό υπολογισμό aggregations σε πεπερασμένα χρονικά τμήματα. Επιπλέον, ο μηχανισμός checkpointing εγγυάται ότι η κατάσταση του συστήματος μπορεί να αποκατασταθεί σε περίπτωση αποτυχίας, χωρίς απώλεια δεδομένων. Η έκδοση που χρησιμοποιήθηκε είναι η 1.9.3 με legacy APIs (`FlinkKafkaConsumer`, `FlinkKafkaProducer`, `KeyedSerializationSchema`), που αντιστοιχεί στις απαιτήσεις του cluster περιβάλλοντος εκτέλεσης.

**Apache Kafka**

Το Apache Kafka επιλέχθηκε ως message broker λόγω του εξαιρετικά υψηλού throughput και της αρχιτεκτονικής αποσύνδεσης παραγωγών/καταναλωτών (decoupling). Κάθε υπηρεσία παράγει ή καταναλώνει δεδομένα ανεξάρτητα χωρίς άμεση σύνδεση μεταξύ τους, καθιστώντας το σύστημα εύκολα επεκτάσιμο. Η δυνατότητα replay — αναπαραγωγή μηνυμάτων από συγκεκριμένο offset — είναι κρίσιμη για την ορθή λειτουργία του Flink Job: κατά την εκκίνηση, το Job διαβάζει το config topic από την αρχή (`earliest`) ώστε να φορτώσει στη μνήμη όλα τα configurations που είχαν υποβληθεί στο παρελθόν. Τρία topics χρησιμοποιήθηκαν: `etl.config.v1` για configurations, `etl.input.v1` για sensor events και `etl.output.v1` για αποτελέσματα.

**Spring Boot**

Το Spring Boot επιλέχθηκε για την υλοποίηση του ETL API Service και του File Producer λόγω της δυνατότητας ταχείας ανάπτυξης (rapid development) REST APIs. Η αυτόματη dependency injection, η ενσωματωμένη υποστήριξη Kafka producer μέσω `spring-kafka` και η αυτόματη διαχείριση lifecycle των beans μειώνουν σημαντικά τον boilerplate κώδικα. Επιπλέον, η ενσωματωμένη υποστήριξη health check endpoints (`/health`) διευκολύνει το container orchestration μέσω Docker Compose.

**MongoDB**

Το MongoDB επιλέχθηκε ως βάση αποθήκευσης αποτελεσμάτων λόγω της schema-flexible αρχιτεκτονικής του. Τα αποτελέσματα των διαφόρων transformation types έχουν εγγενώς διαφορετική δομή: ένα element transformation result περιέχει την επεξεργασμένη εγγραφή αισθητήρα, ενώ ένα aggregation result περιέχει έναν αριθμό και μεταδεδομένα παραθύρου. Σε σχεσιακή βάση δεδομένων αυτή η ετερογένεια θα απαιτούσε πολύπλοκο schema ή πολλαπλούς πίνακες. Το MongoDB δέχεται οποιοδήποτε JSON document χωρίς προκαθορισμένο schema, καθιστώντας το ιδανικό για την αποθήκευση ετερογενών αποτελεσμάτων. Κάθε jobId αντιστοιχεί σε ξεχωριστό Collection, επιτρέποντας αποδοτική αναζήτηση ανά εργασία.

**Docker και Docker Compose**

Το Docker επιλέχθηκε για την containerization όλων των υπηρεσιών, εξασφαλίζοντας πλήρως αναπαραγώγιμα περιβάλλοντα εκτέλεσης. Κάθε microservice εκτελείται σε isolated container με συγκεκριμένες και σταθερές εξαρτήσεις, εξαλείφοντας το κλασικό πρόβλημα ασυμβατότητας περιβαλλόντων ("works on my machine"). Το Docker Compose ορίζει δηλωτικά (declaratively) το σύνολο των υπηρεσιών, τις εξαρτήσεις εκκίνησης και τα environment variables, επιτρέποντας την εκκίνηση ολόκληρης της στοίβας (Zookeeper, Kafka, MongoDB, ETL API, Producer, Flink) με μία εντολή.

**Java**

Η Java επιλέχθηκε ως γλώσσα υλοποίησης διότι αποτελεί την πρωτεύουσα γλώσσα του Apache Flink, εξασφαλίζοντας πλήρη συμβατότητα με το Flink API 1.9.3 και πρόσβαση σε όλες τις δυνατότητές του χωρίς περιορισμούς. Επιπλέον, η χρήση της ίδιας γλώσσας για όλα τα microservices (Flink Job, ETL API, File Producer) απλοποιεί την ανάπτυξη και τη συντήρηση του κώδικα.
