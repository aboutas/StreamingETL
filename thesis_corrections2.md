# Κεφάλαιο 2
# Θεωρητικό υπόβαθρο

## 2.1 Ροές δεδομένων (data streams)

Ως ροές χαρακτηρίζονται οι συνεχείς ακολουθίες γεγονότων, ταξινομημένες χρονικά. Όταν αναφερόμαστε σε ροές δεδομένων εννοούμε τη συνεχή μεταφορά δεδομένων από ένα ή περισσότερα συστήματα, συνήθως με υψηλό ρυθμό, με σκοπό την επεξεργασία και την άντληση χρήσιμων πληροφοριών.

Η επεξεργασία ροών δεδομένων (stream processing) έχει εξελιχθεί σημαντικά τα τελευταία χρόνια, με την ανάπτυξη πολλών εξειδικευμένων λύσεων και πλατφορμών επεξεργασίας δεδομένων, που επιτρέπουν την επεξεργασία σε πραγματικό ή σχεδόν πραγματικό χρόνο, την ομαδική επεξεργασία (batch processing), αλλά και υβριδικές τεχνικές που συνδυάζουν και τα δύο μοντέλα. Ένα τέτοιο εργαλείο είναι και το Apache Flink το οποίο έχει χρησιμοποιηθεί στην παρούσα εργασία.

Παράλληλα, έχουν αναπτυχθεί διάφορες τεχνικές και ολοκληρωμένα συστήματα για την εξαγωγή, επεξεργασία και φόρτωση δεδομένων (Extract, Transform, Load – ETL).

Οι πληροφορίες και τα αποτελέσματα που προκύπτουν από την επεξεργασία ροών δεδομένων είναι καταλυτικές για οργανισμούς και επιχειρήσεις, καθώς επιτρέπουν σε αναλυτές ή τρίτα συστήματα να ενημερώνονται και να λαμβάνουν σημαντικές αποφάσεις σε πραγματικό ή σχεδόν πραγματικό χρόνο. Ενδεικτικά παραδείγματα αποτελούν συστήματα αισθητήρων ή χρηματοοικονομικές εφαρμογές.

## 2.2 Επεξεργασία ροών δεδομένων

Στην επεξεργασία ροών τα δεδομένα είναι θεωρητικά άπειρα και εισέρχονται συνεχώς στο σύστημα. Η επεξεργασία πραγματοποιείται πάνω σε γεγονότα ή μικρές ομάδες γεγονότων καθώς καταφθάνουν. Ως γεγονότα ορίζονται εγγραφές δεδομένων που ρέουν/εισέρχονται στο σύστημα. Οι ροές (Streams) είναι συνεχείς ακολουθίες γεγονότων, ενώ οι μετασχηματισμοί (Transformations) είναι πράξεις που εφαρμόζονται στα γεγονότα, όπως map, filter ή flatMap. Τέλος, η κατάσταση (state) θα μπορούσε να περιγράφει ως η μνήμη, η οποία αφορά ουσιαστικά πληροφορίες που διατηρούνται μεταξύ των γεγονότων.

> **[AD2]:** Αυτά πρέπει να τα εξηγήσεις.

Οι βασικοί τελεστές μετασχηματισμού που χρησιμοποιούνται στην επεξεργασία ροών δεδομένων περιγράφονται αναλυτικά παρακάτω:

**map**: Ο τελεστής map εφαρμόζεται σε κάθε γεγονός της ροής ανεξάρτητα και το μετασχηματίζει σε ένα νέο γεγονός, σε αναλογία ένα-προς-ένα. Δέχεται ένα γεγονός εισόδου και παράγει ακριβώς ένα γεγονός εξόδου. Η λειτουργία του δεν εξαρτάται από άλλα γεγονότα της ροής ούτε διατηρεί κατάσταση μεταξύ διαδοχικών επικλήσεων. Χαρακτηριστικά παραδείγματα χρήσης του περιλαμβάνουν τη μετατροπή μορφής δεδομένων (deserialization JSON → αντικείμενο Java), την εξαγωγή συγκεκριμένων πεδίων από ένα σύνθετο γεγονός ή τον εμπλουτισμό κάθε γεγονότος με επιπλέον πληροφορία.

**filter**: Ο τελεστής filter αξιολογεί κάθε γεγονός της ροής βάσει μιας λογικής συνθήκης (predicate) και αποφασίζει αν θα το αφήσει να περάσει ή θα το απορρίψει. Αν η συνθήκη επιστρέψει true, το γεγονός προωθείται στο επόμενο στάδιο της ροής επεξεργασίας· αν επιστρέψει false, το γεγονός αποκλείεται εξ ολοκλήρου. Ο τελεστής filter είναι stateless: δεν κρατά μνήμη προηγούμενων γεγονότων και κάθε γεγονός αξιολογείται αυτόνομα. Τυπικές χρήσεις περιλαμβάνουν την κατακράτηση μόνο γεγονότων που υπερβαίνουν ένα κατώφλι τιμής, τον αποκλεισμό γεγονότων χαμηλής ποιότητας δεδομένων ή τη δρομολόγηση συγκεκριμένων τύπων αισθητήρων σε ξεχωριστές υπο-ροές.

**flatMap**: Ο τελεστής flatMap αποτελεί γενίκευση του map, καθώς για κάθε γεγονός εισόδου μπορεί να παράγει μηδέν, ένα ή περισσότερα γεγονότα εξόδου. Αυτό τον καθιστά ιδιαίτερα ευέλικτο: μπορεί να λειτουργήσει ως map (παράγει ακριβώς ένα αποτέλεσμα), ως filter (παράγει μηδέν ή ένα αποτέλεσμα) ή ακόμα και ως «explode» operator (παράγει πολλαπλά αποτελέσματα από ένα γεγονός). Στην παρούσα εργασία, ο CoFlatMapFunction αποτελεί επέκταση του flatMap για συνδεδεμένες ροές (connected streams), επιτρέποντας την επεξεργασία δύο ανεξάρτητων ροών δεδομένων με κοινή κατάσταση.

## 2.3 Συστήματα ETL

Στην επιστήμη της πληροφορικής το Extract, Transform, Load (ETL) είναι μια υπολογιστική διαδικασία τριών φάσεων όπου τα δεδομένα αρχικά εισέρχονται από μία ή περισσότερες πηγές ως είσοδος στο σύστημα, μετασχηματίζονται και τέλος φορτώνονται σε μία ή περισσότερες εξόδους. [2]

Στα παραδοσιακά συστήματα ETL η επεξεργασία γίνεται σε μεγάλες ομάδες δεδομένων (batches), με τα δεδομένα να συλλέγονται και να επεξεργάζονται ανά χρονικά διαστήματα που επιλέγει ο χρήστης. Πιο σύγχρονα εργαλεία, όπως είναι και το Apache Flink, επιτρέπουν την επεξεργασία σε πραγματικό χρόνο.

Τα streaming ETL συστήματα επιτρέπουν την επεξεργασία και φόρτωση των δεδομένων σε πραγματικό χρόνο. Η αρχιτεκτονική τους, όπως απεικονίζεται στην Εικόνα 1, αποτελείται από τρία βασικά στοιχεία: πηγή δεδομένων, μηχανή streaming ETL και προορισμό. Πιο αναλυτικά:

- **Πηγή:** Είναι η προέλευση της ροής δεδομένων (π.χ. αισθητήρες, συσκευές IoT, κοινωνικά δίκτυα).
- **Μηχανή Streaming ETL:** Διαβάζει συνεχώς δεδομένα σε πραγματικό χρόνο, τα καθαρίζει, τα φιλτράρει και τα μετασχηματίζει καθώς φτάνουν.

> **[AD3]:** Ποιος; → Διορθώθηκε: "Η μηχανή Streaming ETL περιλαμβάνει:"

Η μηχανή Streaming ETL περιλαμβάνει:
  - **Stream Ingestion:** π.χ. AWS IoT, Kinesis Agent, AWS SDK
  - **Stream Storage:** π.χ. Amazon MSK, Kinesis Data Streams, Apache Kafka
  - **Stream Processing:** π.χ. AWS Lambda για επεξεργασία γεγονότων

Μετά την επεξεργασία, τα δεδομένα φορτώνονται στον προορισμό για περαιτέρω ανάλυση (αποθήκη δεδομένων ή εφαρμογή βασισμένη σε γεγονότα). [3]

Η Εικόνα 1 απεικονίζει τη ροή δεδομένων σε ένα τυπικό streaming ETL σύστημα. Από αριστερά προς τα δεξιά, η πηγή (Source) τροφοδοτεί δεδομένα μέσω των σταδίων Stream Ingestion και Stream Storage σε ένα στάδιο Stream Processing, από το οποίο τα επεξεργασμένα δεδομένα κατευθύνονται στον τελικό προορισμό (Destination). Η αρχιτεκτονική αυτή επιτρέπει την επεξεργασία χαμηλής καθυστέρησης (low-latency), καθώς κάθε γεγονός επεξεργάζεται μόλις εισέλθει στο σύστημα χωρίς να αναμένει τη συγκέντρωση batch.

> **[AD4]:** Αν έχεις αντιγράψει την εικόνα από κάπου, πρέπει να βάλεις πηγή. Επίσης, στο κείμενο ΠΡΕΠΕΙ να αναφέρεσαι και να περιγράφεις όλες τις εικόνες και τους πίνακες.

Εικόνα 1: Αρχιτεκτονική Streaming ETL (Εικόνα από [3])

## 2.4 Apache Kafka

Το Apache Kafka είναι μια πλατφόρμα κατανεμημένης ροής γεγονότων (event streaming) ανοιχτού κώδικα, που χρησιμοποιείται ευρέως για την κατασκευή αγωγών δεδομένων υψηλής απόδοσης, αναλυτική επεξεργασία ροών δεδομένων, ενοποίηση δεδομένων [4].

Το Kafka συνδυάζει τρεις βασικές δυνατότητες:

- Δημοσίευση (write) και εγγραφή (read) σε ροές γεγονότων (events), συμπεριλαμβανομένης της συνεχούς εισαγωγής και εξαγωγής δεδομένων από άλλα συστήματα.
- Αποθήκευση ροής γεγονότων με ανθεκτικό και αξιόπιστο τρόπο για όσο χρονικό διάστημα θέλει ο χρήστης.
- Επεξεργασία ροών γεγονότων τη στιγμή που συμβαίνουν ή και αναδρομικά.

Όλη αυτή η λειτουργικότητα παρέχεται με κατανεμημένο τρόπο, με υψηλή κλιμάκωση, ελαστικότητα, ανθεκτικότητα σε σφάλματα και ασφάλεια. Το Kafka μπορεί να αναπτυχθεί σε φυσικό εξοπλισμό (bare-metal), σε εικονικές μηχανές και σε containers, τόσο σε τοπικές υποδομές (on-premises) όσο και στο cloud [4].

Βασικά στοιχεία της αρχιτεκτονικής του είναι τα ακόλουθα:

- **Topics:** Λογικές δομές στις οποίες αποθηκεύονται οι εγγραφές.
- **Partitions:** Κάθε topic χωρίζεται σε ένα ή περισσότερα partitions ώστε να υπάρχει παραλληλισμός. Κάθε partition είναι ταξινομημένο.
- **Brokers:** Οι κόμβοι του Kafka cluster που αποθηκεύουν τα δεδομένα και εξυπηρετούν τους producers και consumers.
- **Producers:** Εφαρμογές που δημοσιεύουν μηνύματα σε topics.
- **Consumers:** Εφαρμογές που εγγράφονται σε topics και επεξεργάζονται τις εγγραφές.
- **Consumer Groups:** Επιτρέπουν την κατανομή της επεξεργασίας μεταξύ πολλαπλών καταναλωτών.
- **Zookeeper server:** Διαχειρίζεται τον Kafka cluster (σε νεότερες εκδόσεις δεν απαιτείται).

Το Kafka περιλαμβάνει πέντε βασικά APIs:

- **Producer API:** Επιτρέπει στις εφαρμογές να στέλνουν ροές δεδομένων (streams) σε topics μέσα στο Kafka cluster.
- **Consumer API:** Επιτρέπει στις εφαρμογές να διαβάζουν ροές δεδομένων από topics στον Kafka cluster.
- **Streams API:** Επιτρέπει τον μετασχηματισμό ροών δεδομένων από topics εισόδου σε topics εξόδου.
- **Connect API:** Επιτρέπει την υλοποίηση συνδέσεων (connectors) που είτε αντλούν συνεχώς δεδομένα από κάποιο σύστημα ή εφαρμογή προς το Kafka είτε προωθούν δεδομένα από το Kafka προς κάποιο σύστημα ή εφαρμογή-προορισμό.
- **Admin API:** Επιτρέπει τη διαχείριση και τον έλεγχο topics, brokers και άλλων αντικειμένων του Kafka [5].

## 2.5 Apache Flink

Το Apache Flink είναι μια πλατφόρμα και κατανεμημένη μηχανή επεξεργασίας δεδομένων, σχεδιασμένη για την εκτέλεση υπολογισμών με κατάσταση (stateful) τόσο σε συνεχείς όσο και σε πεπερασμένες ροές δεδομένων. Έχει αναπτυχθεί με στόχο να λειτουργεί αποδοτικά σε όλα τα συνήθη περιβάλλοντα συστάδων (clusters) και να εκτελεί υπολογισμούς με ιδιαίτερα υψηλή απόδοση, αξιοποιώντας κυρίως τη μνήμη (in-memory processing) [6].

Το Apache Flink διακρίνεται για την ευελιξία και την αποδοτικότητά του στην επεξεργασία ροών δεδομένων κάθε είδους. Η εκτέλεση μιας εφαρμογής στο Flink βασίζεται σε έναν κατευθυνόμενο άκυκλο γράφο (Directed Acyclic Graph – DAG), όπου κάθε κόμβος αντιστοιχεί σε έναν τελεστή (operator) επεξεργασίας και κάθε ακμή σε μία ροή δεδομένων. Η κατανομή των δεδομένων μεταξύ των τελεστών πραγματοποιείται μέσω μηχανισμών όπως οι keyBy, broadcast και rebalance, επιτρέποντας την υλοποίηση διαφορετικών στρατηγικών διαμερισμού και παραλληλισμού των δεδομένων.

Το περιβάλλον εκτέλεσης (runtime) της Flink αποτελείται από δύο βασικούς τύπους διεργασιών: τον JobManager και έναν ή περισσότερους TaskManagers. Ο JobManager είναι υπεύθυνος για τον προγραμματισμό και τον συνολικό συντονισμό της εκτέλεσης μιας εφαρμογής. Διαχειρίζεται το γράφημα εκτέλεσης του προγράμματος (JobGraph), παρακολουθεί την πρόοδο της επεξεργασίας, εκκινεί ή επανεκκινεί επιμέρους εργασίες (tasks) σε περίπτωση αποτυχίας και διασφαλίζει τη συνέπεια της κατάστασης μέσω του μηχανισμού checkpoints.

Οι TaskManagers (γνωστοί και ως workers) είναι υπεύθυνοι για την πραγματική εκτέλεση των εργασιών ενός dataflow, καθώς και για την προσωρινή αποθήκευση και την ανταλλαγή των ροών δεδομένων μεταξύ των επιμέρους σταδίων επεξεργασίας. Σε κάθε σύστημα Flink πρέπει να υπάρχει τουλάχιστον ένας TaskManager. Η μικρότερη μονάδα χρονοπρογραμματισμού πόρων σε έναν TaskManager είναι το λεγόμενο task slot, ενώ ο συνολικός αριθμός των διαθέσιμων slots καθορίζει τον μέγιστο αριθμό εργασιών που μπορούν να εκτελούνται ταυτόχρονα στο σύστημα (παραλληλισμός).
