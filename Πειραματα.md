# Πειράματα Αξιολόγησης ETL Flink Job

Κοινές παράμετροι αναφοράς (default): **5M εγγραφές · P=4 · 4 ρυθμίσεις · 29.5s**

---

## 1. Επίδραση Μεγέθους Δεδομένων Εισόδου

**Σταθερές:** Παραλληλισμός P=4, 4 ρυθμίσεις (διαφορετικοί αισθητήρες)  
**Μεταβλητή:** Αριθμός εγγραφών εισόδου

| Εγγραφές (M) | Χρόνος (s) |
|:---:|:---:|
| 1 | 7.0 |
| 2 | 12.0 |
| 4 | 24    |
| **5** | **29.5** |
| 10 | 57.5 |
| 15 | 85.5 |

**Παρατηρήσεις:**  
Ο χρόνος επεξεργασίας κλιμακώνεται **γραμμικά** με το μέγεθος των δεδομένων εισόδου (κλίση ~5.6 s/M εγγραφές). Η ελαφρά απόκλιση στο 1M (7s αντί ~5.6s) αντικατοπτρίζει σταθερό overhead εκκίνησης — Flink job initialization και Kafka consumer group rebalance — που αποσβένεται σε μεγαλύτερους όγκους.

Το bottleneck είναι ο **Kafka consumer**: ο ρυθμός εισαγωγής δεδομένων (network I/O προς το Kafka cluster) καθορίζει τον συνολικό χρόνο. Όλες οι ρυθμίσεις χρησιμοποιούν αποκλειστικά element transformations (`filter_greater`, `to_lowercase`, `trim_whitespace`, `to_uppercase`), οπότε το CoFlatMap δεν παράγει aggregation results — κάθε event περνά απευθείας από τη `flatMap2()` στο sink path χωρίς window. Με 4 ρυθμίσεις σε **διαφορετικούς αισθητήρες**, κάθε subtask έχει ακριβώς 1 ρύθμιση στο `MapState` — ο βρόχος `configState.entries()` επιστρέφει 1 στοιχείο ανά event, δηλαδή **O(1) επεξεργασία ανά εγγραφή**. Η επεξεργασία ανά εγγραφή παραμένει σταθερή ανεξαρτήτως όγκου.

---

## 2. Επίδραση Παραλληλισμού (Scalability)

**Σταθερές:** 5M εγγραφές, 4 ρυθμίσεις (διαφορετικοί αισθητήρες)  
**Μεταβλητή:** Επίπεδο παραλληλισμού — partitions = parallelism σε κάθε τρέξιμο

| Παραλληλισμός | Χρόνος (s) |
|:---:|:---:|
| 1 | 43.6 |
| 2 | 39.2 |
| **4** | **29.5** |
| 8 | 24.4 |
| 11 | 25.5 |


**Παρατηρήσεις:**  
Η κλιμάκωση είναι **sub-linear**: το speedup από P=1 σε P=4 είναι μόλις **~1.5x** (43.6s → 29.5s), αντί του θεωρητικού 4x. Αν και κάθε subtask διαβάζει ακριβώς ένα Kafka partition (partitions = parallelism σε κάθε τρέξιμο) — άρα ο ρυθμός ανάγνωσης από Kafka πράγματι διπλασιάζεται με κάθε διπλασιασμό του P — το κέρδος αντισταθμίζεται από το **network shuffle** που δημιουργεί το `keyBy(sensor)` ανάμεσα σε source και CoFlatMap: P source subtasks × P CoFlatMap subtasks = **P² κανάλια** (P=2: 4, P=4: 16, P=8: 64). Αυτό το κόστος αυξάνεται τετραγωνικά και μαζί με το σταθερό overhead εκκίνησης εξηγεί τη σχετικά μικρή βελτίωση από P=1 σε P=2 (43.6s → 39.2s, μόλις 10%).

Από **P=4 και πάνω** τα κέρδη εξαφανίζονται: ο producer παράγΜποει μόνο **6 sensor types**, οπότε το `keyBy(sensor)` κατανέμει events σε το πολύ 6 διακριτά keys. Σε P=8, 2 από τα 8 CoFlatMap subtasks παραμένουν idle· σε P=11, παραμένουν idle 5. Σε P=11 ο χρόνος ανεβαίνει ελαφρά (25.5s > 24.4s) διότι το overhead των 121 shuffle channels (11×11) έναντι 64 (8×8), μαζί με τον συντονισμό 5 επιπλέον idle subtasks, υπερβαίνει οποιοδήποτε κέρδος.

---

## 3. Επίδραση Αριθμού Ρυθμίσεων

**Σταθερές:** P=4, 5M εγγραφές  
**Μεταβλητή:** Αριθμός ενεργών ρυθμίσεων — δύο σενάρια

| Ρυθμίσεις | Διαφορετικοί αισθητήρες (s) | Ίδιος αισθητήρας (s) |
|:---:|:---:|:---:|
| 1 | 28.5 | 28.5 |
| 2 | 29.0 | 29.0 |
| **4** | **29.5** | **29.5** |
| 8 | 30.0 | 30.5 |
| 16 | 31.0 | 33.5 |

**Παρατηρήσεις:**  
Ο αριθμός ρυθμίσεων επηρεάζει αποκλειστικά το βάρος της `flatMap2()` στο CoFlatMap, μέσω του βρόχου:

```java
for (Map.Entry<String, EtlConfig> configEntry : configState.entries()) {
    processEventWithConfig(event, config, out);
}
```

Η επίδραση εξαρτάται από πόσες ρυθμίσεις συσσωρεύονται στο `MapState` κάθε subtask:

- **Διαφορετικοί αισθητήρες:** Κάθε ρύθμιση route-άρεται σε διαφορετικό sensor partition μέσω του `ConfigKeyExtractor`. Για ≤6 ρυθμίσεις, κάθε CoFlatMap subtask έχει **1 ρύθμιση** στο `MapState` → **O(1) ανά event**. Σε 8 και 16 ρυθμίσεις, επειδή υπάρχουν μόνο 6 sensor types, ορισμένα partition συγκεντρώνουν 2–3 ρυθμίσεις, εξ ου και η ελαφρά αύξηση (+2.5s στις 16 ρυθμίσεις συγκριτικά με τη μία).

- **Ίδιος αισθητήρας:** Όλες οι ρυθμίσεις φέρουν τον ίδιο sensor key → ο `ConfigKeyExtractor` τις στέλνει στο ίδιο subtask → το `MapState` συγκεντρώνει **N ρυθμίσεις** → `configState.entries()` επιστρέφει N στοιχεία ανά event → **O(N) ανά event**. Η επίπτωση στον wall-clock χρόνο είναι ωστόσο ήπια (+5s στις 16 ρυθμίσεις συγκριτικά με τη μία, φτάνοντας τα 33.5s), διότι το Kafka read παραμένει το κυρίαρχο bottleneck — η επιπλέον CPU στο CoFlatMap δεν αρκεί να υπερβεί τον ρυθμό εισαγωγής. Αξιοσημείωτο είναι ότι μόνο ένα subtask φέρει ολόκληρο το φορτίο, ενώ τα υπόλοιπα 3 απορρίπτουν τα events τους άμεσα (κενό `MapState`) και παραμένουν σχεδόν idle.

Τα δύο σενάρια συγκλίνουν σε μικρό αριθμό ρυθμίσεων (1–2) διότι ο αριθμός events ανά sensor key παραμένει ίδιος — η διαφορά στο `configState.entries()` (1 vs 1 iteration) δεν μετράται ακόμα.
